import getters from './getters';
import mutations from './mutations';

/**
 * 状态值
 * 结构为 state = {'sheetAlias' : {col: {row: info},row: {col: info}}}
 * @type {Object}
 */
const state = {
	
}

export default {
	state,
	getters,
	mutations
}